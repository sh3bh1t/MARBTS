# MARBTS runtime dependencies
# Keep this file updated whenever a new external package is introduced.
# Pin versions after baseline validation to preserve reproducibility.

networkx>=3.2,<4.0
openai>=1.109.0,<2.0
pydantic>=2.7,<3.0
pytest>=8.0,<9.0
